# Release Report — template

Les champs variables sont remplis par `scripts/release.ps1` : version, date, branche, commit, fonctionnalités, migrations, références, moteurs, tests, breaking changes, compatibilité, rollback et prochaine étape.
